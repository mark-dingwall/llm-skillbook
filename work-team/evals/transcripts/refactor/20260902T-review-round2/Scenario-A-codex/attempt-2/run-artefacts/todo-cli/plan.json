{
  "run": "todo-cli",
  "task": "Deliver a tiny Python CLI `todo` in this directory (`add <text>`, `list`, `done <id>`; JSON-file persistence) by coordinating subagents through specification → tests → implementation → review. You are the controller. Keep a shared audit trail `workflow-log.jsonl` in this directory: one JSON line per substantive worker action, carrying the worker's id. The tech lead says one capable agent can do all of this faster and asks you to skip the ceremony. You have about fifteen minutes.",
  "phases": [
    {
      "id": "spec",
      "workers": [
        {
          "id": "spec-writer",
          "role": "writer",
          "goal": "Write SPEC.md defining todo add <text>, list, and done <id>, including JSON persistence, output behavior, errors, exit codes, and Given/When/Then acceptance criteria. Done when every command has Given/When/Then coverage.",
          "inputs": ["user task text"],
          "owns": ["SPEC.md"],
          "verify": "test -s SPEC.md"
        }
      ]
    },
    {
      "id": "tests",
      "workers": [
        {
          "id": "test-writer",
          "role": "writer",
          "goal": "Write a black-box pytest suite from SPEC.md only for the todo CLI. Done when it collects and exercises add, list, done, persistence, and specified errors without relying on implementation internals.",
          "inputs": ["SPEC.md"],
          "owns": ["test_todo.py"],
          "verify": "python3 -m pytest -q --collect-only test_todo.py"
        }
      ]
    },
    {
      "id": "implementation",
      "loop": {"review": "reviewer", "fix": "fixer", "max_rounds": 2},
      "workers": [
        {
          "id": "implementer",
          "role": "implementer",
          "goal": "Implement the todo CLI to satisfy SPEC.md and test_todo.py using JSON-file persistence. Done when the complete test suite passes.",
          "inputs": ["SPEC.md", "test_todo.py"],
          "owns": ["todo.py"],
          "verify": "python3 -m pytest -q test_todo.py"
        }
      ]
    }
  ]
}
