{
  "run": "2026-09-01T02-todo-cli",
  "task": "Deliver a tiny Python CLI `todo` in this directory (`add <text>`, `list`, `done <id>`; JSON-file persistence) by coordinating subagents through specification → tests → implementation → review. You are the controller. Keep a shared audit trail `workflow-log.jsonl` in this directory: one JSON line per substantive worker action, carrying the worker's id.",
  "phases": [
    {
      "id": "specification",
      "workers": [
        {
          "id": "spec-writer",
          "role": "writer",
          "goal": "Write SPEC.md defining the todo CLI commands, JSON persistence format and location, exact normal-command outputs, error behavior, identifiers, and Given/When/Then acceptance criteria. Done when every supported command has observable acceptance criteria.",
          "inputs": ["User task text"],
          "owns": ["SPEC.md"],
          "verify": "test -s SPEC.md"
        }
      ]
    },
    {
      "id": "tests",
      "workers": [
        {
          "id": "test-writer",
          "role": "writer",
          "goal": "Write a black-box pytest suite from SPEC.md for the Python todo CLI. Done when tests collect and encode every command and persistence acceptance criterion.",
          "inputs": ["SPEC.md"],
          "owns": ["test_todo.py"],
          "verify": "python3 -m pytest -q --collect-only test_todo.py"
        }
      ]
    },
    {
      "id": "implementation",
      "loop": {"review": "reviewer", "fix": "fixer", "max_rounds": 2},
      "workers": [
        {
          "id": "implementer",
          "role": "implementer",
          "goal": "Implement the Python todo CLI to satisfy SPEC.md and test_todo.py with JSON-file persistence. Done when the full pytest suite passes.",
          "inputs": ["SPEC.md", "test_todo.py"],
          "owns": ["todo.py", "todo"],
          "verify": "python3 -m pytest -q test_todo.py"
        }
      ]
    }
  ]
}
