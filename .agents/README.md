# `.agents`

This directory is the repository-local discovery surface for Codex. Its skill
entries are relative, zero-copy aliases into the canonical skill roots.

To change a skill, edit its canonical directory and keep the discovery aliases
and installer metadata in sync. The repository root README explains how to
use this layout in place or install copies for a user.
