You are dispatched as `multi-review-reviewer` for run <RUN_ID>.

Read the prompt from this file (absolute path; do NOT modify):
  <PROMPT_PATH>

Produce a structured review per your agent definition.

Do NOT call Write or any other tool to persist your output — your tool grant
does not include Write, and the host captures the text of your final assistant
message directly. End your message with the full review markdown, nothing else
(no preamble, no "Review written to ..." line, no tool-call narration).
