# Claude plugin hooks

This directory registers narrowly scoped Claude Code hooks shipped by the
plugin. The work-team capture hook runs only for its dedicated worker agent and
is inert outside an active `.work-team/` run.

Install the repository as a Claude plugin using the instructions in the root
[README](../README.md), or use `install.py` for a user-scoped copy.
