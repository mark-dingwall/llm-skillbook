EVIDENCE & SEVERITY CONTRACT CRITIC. Critique the reviewer-facing contract: loop-prompt-draft.md's addendum together with requesting-code-review/code-reviewer.md, the base template it extends. Hunt for: undefined or ambiguous boundaries (the Important/Minor boundary gets the blocking question — is Critical/Important defined anywhere?); clauses a reviewer cannot actually comply with, or whose compliance cannot be checked; edge cases in the CONFIRMED/PLAUSIBLE rule; whether the design-finding path ("name a concrete future change the design makes harder") is exploitable or too strict; whether the failing-scenario requirement wrongly excludes real finding classes (security hardening with no single failing input, performance regressions, maintainability rot); and tensions between the addendum and the base template it extends (duplicated or conflicting instructions a reviewer must reconcile).
