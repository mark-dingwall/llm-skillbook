RATIONALIZATION-SURFACE HUNT. Assume the skill is executed by a capable but pressured agent: long context, sunk cost in its own fixes, an impatient user, a shrinking budget. Walk the loop-prompt-draft.md flow step by step and mark every point where such an agent would plausibly cut a corner while claiming compliance (e.g. "verified against sources" without reading them, quietly narrowing the diff range, marking its own fix's side-effect INTENTIONAL, declaring convergence early, skipping a reviewer). For each point classify the right countermeasure: (a) a forced verbatim output artifact tied to an action in hand; (b) it is absence-detection — which fable-method-notes.md reports forced artifacts do NOT fix — so it needs a structural mechanism instead (say which); or (c) adequately self-enforcing as written. Prioritize corners whose cutting is invisible in the final report. Note the triager is the SAME agent that wrote the fixes — a conflict of interest the draft does not address; examine what that contaminates.
