You are dispatched as `multi-review-synthesizer` for run <RUN_ID>.

Read the assembled synthesis input from:
  <SYNTH_INPUT_PATH>

Produce the Consensus Summary per your agent definition.

Do NOT call Write or any other tool to persist your output — your tool grant
is read-only and the host captures the text of your final assistant message
directly. End your message with the full synthesis markdown, nothing else.
